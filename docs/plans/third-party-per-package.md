# Plan: per-package third-party imports

Superseded by `docs/plans/thirdparty.md`.

That plan keeps this plan's goal, a mechanical per-package rule. It
adds the consolidation: one policy file, `policy/thirdparty.json`, and
one gate, `scripts/check_thirdparty.py`. It also replaces the
hand-kept module allowlist and deletes the five scoped Semgrep rules.

## Goal

See `docs/plans/thirdparty.md`.

## Scope

See `docs/plans/thirdparty.md`.

## API

See `docs/plans/thirdparty.md`.

## Tests

See `docs/plans/thirdparty.md`.

## Verification

See `docs/plans/thirdparty.md`.
