// Package a2aloopback ships a real gRPC A2A test-server fixture,
// Loopback. No production code may import it; it exists to run inside
// another package's own tests, the same convention durablefence uses.
// See durablefence/doc.go.
package a2aloopback
