{
  "denylist": [
    {
      "file": "grpc.go",
      "snippet": "!ok || task == nil",
      "reason": "typed-nil guard in grpcTransport.Send: mutating || to && stays observationally equivalent for every reachable fake response, so no in-package test kills it"
    }
  ],
  "floor": 95
}
